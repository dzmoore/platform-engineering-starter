{
  "$schema": "https://raw.githubusercontent.com/jetify-com/devbox/0.13.7/.schema/devbox.schema.json",
  "packages": [
    "colima@latest",
    "docker@latest",
    "kind@latest",
    "kubectl@latest",
    "kubernetes-helm@latest",
    "just@latest",
    "kcl@latest",
    "kyverno-chainsaw@latest",
    "crossplane-cli@latest",
    "yq-go@latest",
    "google-cloud-sdk@latest"
  ],
  "shell": {
    "init_hook": [
      "alias 'k'=\"$(which kubectl)\"",
      "if [ -n \"$BASH_VERSION\" ]; then source <(kind completion bash); source <(kubectl completion bash); source <(helm completion bash); complete -F __start_kubectl k; source <(just --completions bash); source <(chainsaw completion bash); elif [ -n \"$ZSH_VERSION\" ]; then source <(kind completion zsh); source <(kubectl completion zsh); source <(helm completion zsh); compdef __start_kubectl k; source <(just --completions zsh); source <(chainsaw completion zsh); else echo 'Unknown shell'; fi"
    ],
    "scripts": {
      "test": ["echo \"Error: no test specified\" && exit 1"]
    }
  }
}
